name: Auto Labeler

on:
  pull_request: # 테스트를 위해 pull_request_target 대신 pull_request 사용
    types: [opened, reopened, edited, synchronize]

jobs:
  label:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Labeler (Title-based)
        uses: actions/github-script@v7
        with:
          script: |
            const title = context.payload.pull_request.title.toLowerCase();
            const prNumber = context.payload.pull_request.number;
            const labels = [];

            const rules = {
              'feat': /^feat:|^feat\(/,
              'fix': /^fix:|^fix\(/,
              'docs': /^docs:|^docs\(/,
              'style': /^style:|^style\(/,
              'refactor': /^refactor:|^refactor\(/,
              'test': /^test:|^test\(/,
              'chore': /^chore:|^chore\(/,
              'design': /^design:|^design\(/
            };

            for (const [label, regex] of Object.entries(rules)) {
              if (regex.test(title)) {
                labels.push(label);
              }
            }

            if (labels.length > 0) {
              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                labels: labels
              });
              console.log(`Added labels ${labels} to PR #${prNumber}`);
            } else {
              console.log(`No matching labels found for title: "${title}"`);
            }
